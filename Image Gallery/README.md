# Image Gallery 🎨

A responsive and interactive image gallery built using **HTML and CSS**.  
This project showcases a creative use of **CSS Grid**, **hover effects**, and **filters** to make images come alive on interaction.

## ✨ Features
- 3×3 grid layout for image display  
- Black & white images by default — colorize on hover  
- Smooth enlarge (zoom-in) effect with transition  
- Text overlay appears with hover animation  
- Uses CSS properties like `filter`, `transform`, and `transition` for stylish interactivity  
- Fully responsive layout that adapts to screen size  

## 🧰 Tech Stack
- **HTML5** — for structure  
- **CSS3** — for styling, transitions, and hover animations  

## 🖼️ Preview
Hover over images to see the transformation from grayscale to color with text overlay.  
